module.exports = function(Cartoparty) {
  // Cartoparty.afterRemote('records.create', function(context, affectedModelInstance, next) {
  //   affectedModelInstance.userId = context.req.accessToken.userId;
  // }, function(err, response) {
  //   if (err) console.error(err);
  //   next();
  //   }
  // );
};
